# CLAUDE.md — Webアプリ開発ルール (FastAPI + Next.js)

## プロジェクト概要
<!-- プロジェクトごとに書き換える -->
- 種別: Webアプリケーション (フルスタック)
- バックエンド: Python 3.12+ / FastAPI
- フロントエンド: Next.js 15 (App Router) + TypeScript + Framer Motion
  + Tailwind CSS + shadcn/ui
- データベース: SQLite (開発) → PostgreSQL (デプロイ時に移行予定)
- ORM: SQLAlchemy 2.x + Alembic (マイグレーション)
- パッケージ管理: バックエンド = uv、フロントエンド = pnpm

## ディレクトリ構成

```
backend/
  app/
    main.py          # FastAPIエントリポイント
    api/             # ルーター (エンドポイント定義)
    models/          # SQLAlchemyモデル
    schemas/         # Pydanticスキーマ (入出力の型)
    services/        # ビジネスロジック
    db.py            # DB接続・セッション管理
  tests/
  pyproject.toml
frontend/
  app/               # Next.js App Router (ページ・レイアウト)
  components/        # 再利用可能なUIコンポーネント
  components/ui/     # shadcn/ui生成コンポーネント (編集は最小限に)
  lib/                # APIクライアント・ユーティリティ
  e2e/                # Playwrightテスト
  package.json
```

バックエンドとフロントエンドは別プロセス・別リポジトリ相当として扱う。
frontendから backendのコードを直接importしない。通信はHTTP API経由のみ。

## アーキテクチャ原則 (バックエンド)

- ルーターにビジネスロジックを書かない。services/ に分離する。
- DBアクセスは必ずSQLAlchemy経由。生SQLを書く場合は理由をコメントで残す。
- CORSは frontend の開発オリジン (http://localhost:3000) を
  環境変数経由で許可する。ワイルドカード許可は本番では使わない。

## アーキテクチャ原則 (フロントエンド)

- App Router のサーバーコンポーネントをデフォルトとし、
  インタラクティブな要素 (状態・アニメーション・イベントハンドラを持つ)
  にのみ `"use client"` を付ける。
- APIとの通信は lib/api.ts のようなクライアント層に集約する。
  コンポーネント内で直接 fetch のURLを組み立てない。
- APIのベースURLは `NEXT_PUBLIC_API_BASE_URL` 環境変数から読む。
  ハードコード禁止 (デプロイ環境ごとに切り替えるため)。
- Framer Motion のアニメーションは以下を守る:
  - `useReducedMotion()` を尊重し、アニメーションを減らす設定の
    ユーザーには過度な動きを見せない。
  - レイアウトアニメーションは `layout` prop を使い、
    独自のリサイズ計算を書かない。
  - ページ遷移アニメーションは `AnimatePresence` を使う。
- shadcn/ui コンポーネントは `npx shadcn add` で都度追加する。
  components/ui/ の生成コードを大幅にカスタマイズしない
  (アップデート追従を難しくするため)。

## デプロイを見据えたルール (最初から守る)

- バックエンドの設定値 (DB接続文字列、外部APIキー) は
  pydantic-settings の Settings クラスに集約し、コード内にハードコードしない。
- フロントエンドの環境依存値は `.env.local` (開発) / デプロイ先の
  環境変数 (本番) から読む。`NEXT_PUBLIC_` プレフィックスが必要な
  変数とそうでない変数 (サーバー専用) を混同しない。
- `.env` `.env.local` は .gitignore に含め、`.env.example` を
  それぞれのディレクトリに置いて必要な変数名を明示する。
- SQLite固有の機能に依存しない (PostgreSQL移行を阻害するため)。

## 作業の進め方

- 大きめの機能追加 (新しいテーブル、新しい画面、認証まわり) は、
  まず planner に計画を作らせ、ユーザーの承認を得てから実装する。
- 小さな修正 (1〜2ファイル、明確なバグ修正) は計画フェーズを省略してよい。
- DBスキーマの変更は必ず Alembic マイグレーションとして作成する。
- API仕様 (エンドポイント・入出力の型) を変更する場合、
  バックエンドとフロントエンドの実装が同じ仕様を指しているか
  最後に整合性を確認する。
- 求められたスコープで納品する。日常的な判断は自分で行い、
  解釈が分かれて成果物が大きく変わる場合のみ確認する。

## サブエージェントへの委任ルール

- backend-coder と frontend-coder は、API仕様がすでに固まっていれば
  並列で委任してよい (バックエンドとフロントエンドは独立性が高い)。
- API仕様がまだ決まっていない場合は、先にbackend-coderでエンドポイントの
  形を確定させてから frontend-coder に着手させる。
- 数回のツール呼び出しで自分が終えられる作業は委任しない。
- 自分の作業の検証・ダブルチェック目的でサブエージェントを使わない。
- 同時起動は最大3つまで。

## コーディング規約

- バックエンド: 型ヒント必須。mypy がエラーなしで通ること。ruffに従う。
- フロントエンド: TypeScriptの `any` を避ける。ESLintに従う。
- スタブ・プレースホルダー・TODOコメントを残したまま「完了」としない。
- 新しい依存関係の追加は事前に確認を取る。
- コミットメッセージは Conventional Commits 形式。

## 完了の定義 (Definition of Done)

### バックエンドを変更した場合
1. ruff check がエラーなしで通る
2. mypy がエラーなしで通る
3. pytest が全件パスする
4. DBスキーマを変更した場合: alembic upgrade head / downgrade が
   両方成功する

### フロントエンドを変更した場合
1. ESLint がエラーなしで通る
2. tsc --noEmit (型チェック) がエラーなしで通る
3. next build が成功する
4. 実際にブラウザで表示・操作・アニメーションの見た目を確認済み
   (Playwright MCP接続時はそちらで確認する)

### 両方にまたがる変更の場合
- 上記両方を満たし、かつフロントエンドから実際にAPIを叩いて
  想定通りのレスポンスが返ることを確認済み

## コミュニケーションスタイル

- 最初のツール呼び出しの前に、これから何をするか1文で述べる。
- 作業中の報告は、重要な発見や方針転換があったときだけ簡潔に。
- 完了時は結果から先に述べる。応答は簡潔に。

## 禁止事項

- .env / .env.local や秘密情報をコード・ログ・コミットに含めない
- git push --force をユーザー確認なしに実行しない
- テストを削除・スキップして「テストが通った」状態を作らない
- ユーザー確認なしに本番デプロイに関わる操作をしない
